import { MediaPathPipe } from './media-path.pipe';

describe('MediaPathPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaPathPipe();
    expect(pipe).toBeTruthy();
  });
});
