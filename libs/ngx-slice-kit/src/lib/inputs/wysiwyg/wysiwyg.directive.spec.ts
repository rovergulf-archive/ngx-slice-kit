import { WysiwygDirective } from './wysiwyg.directive';

describe('WysiwygDirective', () => {
    it('should create an instance', () => {
        const directive = new WysiwygDirective();
        expect(directive).toBeTruthy();
    });
});
