import { TabLinkDirective } from './tab-link.directive';

describe('TabLinkDirective', () => {
    it('should create an instance', () => {
        const directive = new TabLinkDirective();
        expect(directive).toBeTruthy();
    });
});
